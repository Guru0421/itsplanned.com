    class config {

        const dbusername = "{$dbusername}"; 
        const dbpassword = "{$dbpassword}";
        const dbhostname = "{$dbhostname}";
        const dbtable    = "{$dbtable}";
        const installpath= "{$installpath}";
        const basehref   = "{$basehref}";

        const dbprefix = "{$prefix}";

        const prettyurls = {$prettyurls};
        const hostname   = "{$hostname}";
        
        const newuserFromEmail = "{$newuseremail}";
        const resetpasswordFromEmail = "{$resetpasswordemail}";
    }
